<h1>EAT UP!</h1>

<h2>Burgers that need to be eaten!</h2>

<ul>
  {{#each burger}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Eaten burgers</h2>

<ul>
  {{#each burger}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Add a Burger you want to eat</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name</label>
    <input type="text" id="ca" name="name">
  </div>
  
  <div class="form-group">
    <label for="sleepy">Have you eaten this burger yet?</label><br>
    <input type="radio" name="devoured" value="1" checked> Eaten<br>
    <input type="radio" name="sleepy" value="0"> Not Eaten
  </div>

  <button type="submit">Add Burger</button>
</form>
